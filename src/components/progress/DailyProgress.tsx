"use client"

import { Check, X, Clock } from 'lucide-react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function DailyProgress({ progress }: { progress: any }) {
  return (
    <Card className="bg-gray-800">
      {/* ... daily progress UI ... */}
    </Card>
  );
} 