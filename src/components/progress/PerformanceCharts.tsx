"use client"

import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip } from 'recharts';

export function PerformanceCharts({ timeData, dayData }: { timeData: any; dayData: any }) {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
      {/* ... charts ... */}
    </div>
  );
} 